# EVM-machine
A responsive Electronic Voting Machine (EVM) web application built using HTML, CSS, and JavaScript. Users can cast votes for different candidates, view live vote counts, prevent multiple voting, and simulate a real-world voting experience through an interactive interface.
